apiVersion: v1
kind: Service
metadata:
  name: {{ include "halopsa-mcp.fullname" . }}
  labels:
    {{- include "halopsa-mcp.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "halopsa-mcp.selectorLabels" . | nindent 4 }}
